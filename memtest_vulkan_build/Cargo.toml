[package]
edition = '2024'
name = 'memtest_vulkan_build'
version = '0.0.0'

[dependencies]
quote = '1.0'
syn = '2.0'

[dependencies.naga]
default-features = false
features = [
    'wgsl-in',
    'spv-out',
]
version = '0.10.1'

[lib]
proc-macro = true
